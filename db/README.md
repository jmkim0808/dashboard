# 데이터베이스

파워넷 ESG 데이터 입력플랫폼의 데이터 구조. 대상은 **Cloudflare D1 (SQLite)** 이다.

| 파일 | 내용 |
|---|---|
| `schema.sql` | 테이블 15개 · 트리거 7개 · 뷰 3개 |
| `seed.sql` | 기준정보 — 법인 3, 사업장 3, 역할 13, 지표 49, 담당배정 111 |
| `factors.sql` | 배출계수 — 계수 정책과 3개 지역 전력 배출계수 |
| `verify.py` | 설계 원칙이 DB 수준에서 강제되는지 검증 (23개 항목) |

---

## 적용

### 1. D1 데이터베이스 생성 (최초 1회)

```bash
npx wrangler d1 create powernet-esg
```

출력된 `database_id` 를 `wrangler.toml` 의 해당 항목에 붙여 넣는다.

### 2. 스키마와 기준정보 적용

```bash
# 로컬 (내 PC에서 먼저 확인)
npm run db:local        # schema.sql + seed.sql
npm run db:factors      # factors.sql

# 운영 (확인 후)
npm run db:remote
npm run db:factors:remote
```

### 3. 확인

```bash
npx wrangler d1 execute powernet-esg --local \
  --command="SELECT entity_code, COUNT(*) FROM v_expected_entry WHERE period_type='monthly' GROUP BY 1"
```

법인마다 **29** 가 나오면 정상이다. 해외법인 담당자 1인이 매월 입력할 항목 수다.

### 4. 제약 검증

```bash
python3 db/verify.py
```

`통과 23 / 실패 0` 이 나와야 한다. **schema.sql 을 수정한 뒤에는 반드시 돌린다.**

---

## 이 스키마가 DB 수준에서 강제하는 것

설계 원칙이 주석에만 있고 제약에는 없으면 언젠가 깨진다. 아래는 코드가 아니라 **DB가** 막는다.

| 원칙 | 강제 방식 |
|---|---|
| **R84** 개인정보 Zero | 이름·사번·이메일·연락처 컬럼이 **존재하지 않는다**. `verify.py`가 컬럼명을 스캔해 확인 |
| **R85** 사람이 아니라 역할을 기록 | 모든 행위자 컬럼이 `role(code)` 참조. 이메일 매핑은 Cloudflare Access에만 |
| **R86** S 지표 증빙 차단 | `trg_evidence_policy_none` — 급여대장·재해조사표 업로드가 **거부**된다 |
| **R87** 자유 텍스트 없음 | 미확보 사유는 `unavailable_reason` FK, 반송 사유는 CHECK 4개 코드 |
| **R67** 부담당자 필수 | `metric_assignment.backup_role NOT NULL` — 1인 의존 배정이 저장되지 않는다 |
| **R69** 계산지표 입력 불가 | `trg_entry_no_calculated` |
| **D-1** 원본값 불변 | `value_raw` + `unit_raw` 저장, 환산은 `v_entry`가 계산 |
| **D-2** 미입력 ≠ 미확보 | `status` CHECK 3개 — 미확보는 값이 NULL이고 사유가 필수 |
| **D-4** 물리삭제 없음 | `trg_entry_no_delete`, `trg_evidence_no_delete` |
| **R58** 변경은 이력으로 | `trg_entry_audit_value`, `trg_entry_audit_status` |
| **R59** 계수 불변 | `trg_factor_immutable` — 수정 대신 새 버전 등록 |
| **R61** 임대 배분 근거 | `site` CHECK — 임대인데 배분기준·비율·근거가 없으면 저장 거부 |
| **R64** 비율지표 재계산 | `metric.numerator_codes` / `denominator_codes` / `multiplier` — 비율값을 평균하지 않는다 |

---

## 설계 메모

### 지표 하나 = 숫자 하나

성별·고용형태 세분화를 차원(dimension) 테이블로 만들지 않고 **지표를 나눴다.**
`S03 임직원수(남)` / `S04 임직원수(여)` 처럼 둔다.

차원 테이블을 쓰면 입력 화면·집계·비율계산이 모두 복잡해진다. 지표를 나누면
입력자는 칸 하나에 숫자 하나만 넣고, 비율은 전부 계산지표가 만든다.

### 비율지표를 데이터로 표현한 이유

```
값 = (Σ numerator_codes) / (Σ denominator_codes) × multiplier
```

이 한 줄로 비율·집약도 8개가 전부 표현된다.

```
이직률        = (S06) / (S01,S02) × 100
재해 도수율    = (S08) / (S10) × 1,000,000
1인당 교육시간  = (S11,S12) / (S01,S02) × 1
배출 집약도    = (E-C3,E-C4) / (P03) × 100        -- tCO2eq/억원
에너지 집약도  = (E-C1)      / (P03) × 100,000    -- GJ/억원
```

배수(`multiplier`)는 읽을 수 있는 자릿수를 만드는 장치다. 배수 없이 두면
집약도가 `0.0000027` 로 나와 보고서에 쓸 수 없고, 화면에서 "0"과 "미산정"이 구별되지 않는다.

**"3법인 이직률의 평균 ≠ 그룹 이직률"** 이라는 실무에서 가장 흔한 오류가
구조적으로 발생할 수 없다. 분자·분모를 각각 합산한 뒤 나누기 때문이다.
새 비율지표를 추가할 때도 코드 수정이 아니라 마스터 한 줄 추가로 끝난다 (R73).
실제 차이는 `npm run g3` 의 [4]절에서 확인할 수 있다 (집약도 약 60% · 도수율 약 16% 차이).

**금액 지표의 분모는 통화 환산 없이 합산하지 않는다.** `unit_standard` 가
`현지통화` 로 시작하는 지표(P03 매출액)는 법인별 연평균환율로 원화 환산한 뒤 합산한다.
환율이 등록되지 않은 통화가 하나라도 있으면 값을 내지 않고 `missing_fx` 로 표시한다 —
KRW·CNY·VND 를 그대로 더하면 숫자는 나오지만 아무 의미가 없다 (R65).

### 산정 결과 테이블 (`calc_result`) 은 삭제하고 다시 넣는다

`calc_result` 의 UNIQUE 제약에는 `site_id` 가 들어 있고, 법인 합산 행은 그 값이 NULL 이다.
SQLite 는 UNIQUE 안의 NULL 을 **서로 다른 값으로 취급**하므로 `ON CONFLICT` 가 걸리지 않는다.
그대로 두면 재산정할 때마다 행이 쌓여 연간 합계가 실행 횟수만큼 커진다.
따라서 `saveMonthResult()` 는 **같은 키를 삭제한 뒤 삽입**한다.
`calc_result` 는 원본이 아니라 언제든 다시 만들 수 있는 산출물이므로
삭제 금지 원칙(D-4)의 대상이 아니다. 원본은 `entry` 에 그대로 있다.

### 반올림은 한 방향으로만 누적한다

월별 산출값을 반올림하고, **그 반올림한 값을 더해** 연간값과 그룹 합계를 만든다.
반올림 전 값으로 합산하면 보고서에 인쇄된 3법인 수치를 독자가 더했을 때
합계와 0.01 이 달라지고, 그 질문에는 답할 방법이 없다.

### 미확보 항목의 승인 상태는 `status` 가 아니라 `approved_at` 으로 본다

`entry` 에는 `CHECK (status NOT IN ('entered','approved','closed') OR value_raw IS NOT NULL)`
가 걸려 있다. 값이 없는 미확보 항목은 `status='approved'` 가 될 수 없고, **되어서도 안 된다.**
승인된 미확보가 승인된 값과 같은 상태가 되면, 보고서를 쓸 때 그 칸이 빈 이유를
설명할 수 없다 (D-2). 그래서 미확보 항목은 승인·확정 시각만 기록하고
상태는 끝까지 `unavailable` 로 남는다.

### 단위 환산

`metric_unit_override` 가 법인별 입력 단위를 고정한다. 심양 전력은 `万kWh` 로 받고
`factor_to_standard = 10000` 으로 kWh 환산한다. **담당자는 환산하지 않는다** (P-1, P-5).

EP1의 "만kWh 사건"이 발생할 수 없는 지점이다.

### 배출량 산정을 데이터로

`metric.factor_type` + `metric.ghg_scope` + `factor` 테이블로 산정이 돌아간다.
`calc.js` 에 지표 코드나 계수값을 하드코딩하지 않는다.

→ 계수가 개정되거나 연료 항목이 추가돼도 **마스터 화면에서 처리**된다 (R73).
   이것이 유지보수 1인 의존 리스크의 핵심 완화책이다.

---

## 배출계수 정책 (2026-09-17 확정)

보고서 발간 시점의 **국제 최신 기준**을 따른다. 고객사가 다른 계수를 요구하면
그 기준의 계수 세트를 **새 version 으로 추가**하고 산출 시 선택한다.

이 정책이 가능한 이유는 **원본 사용량이 계수와 분리되어 있기 때문**이다.
`entry.value_raw` 는 불변이고 계수는 별도 테이블에 버전으로 관리되므로,
계수가 바뀌어도 사용량은 그대로이고 재산정만 하면 된다. (D-1 / D-3 / R59)

> 따라서 지금 급한 것은 정확한 계수가 아니라 **정확한 사용량**이다.

### 등록된 계수 (v2026.1)

| 구분 | 지역 | 값 | 기준 | 출처 · 고시일 |
|---|---|---|---|---|
| 전력 배출계수 | KR | 0.4173 tCO2eq/MWh | 2023년도 | 기후에너지환경부 온실가스종합정보센터 · 2025-12-17 |
| 전력 배출계수 | CN | 0.5306 kgCO2/kWh | 2023년 | 생태환경부·국가통계국 공고 · 2025-12-31 |
| 전력 배출계수 | VN | 0.6592 tCO2/MWh | 2023년 | MONRE 기후변화국 · 2024-12 |
| 순발열량 | GLOBAL | 0.0000036 TJ/kWh | 물리 상수 | SI 단위 환산 (1 kWh = 3.6 MJ) |

DB에는 지표 E01의 표준단위(kWh)에 맞춰 **tCO2/kWh 단위로 저장**한다.
산정이 "사용량 × 계수" 곱셈 하나로 끝나므로 코드에 단위 변환을 두지 않는다.

**GWP는 IPCC AR6(100년) 채택** — CH4 27.9 · N2O 273.
GHG Protocol이 최신 IPCC 평가보고서 값 사용을 권고하며 CDP·CSRD·SBTi·ISSB가 AR6를 요구한다.

> 단 위 전력 계수는 각국 고시가 이미 CO2 또는 CO2eq로 환산한 값이므로 GWP를 다시 적용하지 않는다.
> `gwp_set` 컬럼에는 그 고시가 사용한 기준을 기록한다(한국은 AR5).
> AR6는 Scope 1 연료 연소의 CH4·N2O 산정에 적용된다.

### 주의 사항 두 가지

**중국은 계수가 두 종류다.** `电力碳足迹因子`(전력 탄소발자국 계수, 2024년 0.5777 kgCO2e/kWh)는
전주기(LCA) 기반으로 **제품 탄소발자국(PCF)용**이다. Scope 2에는
`电力二氧化碳排放因子`(전력 CO2 배출계수, 2023년 0.5306 kgCO2/kWh)를 쓴다. 혼동하면 배출량이 9% 틀어진다.

**한국 계수는 이제 매년 갱신된다.** 갱신 주기가 3년에서 1년으로 단축됐으므로,
매년 새 version 등록이 필요하다. 기존 계수는 수정되지 않으니 이미 제출한 수치는 그대로 재현된다.

---

## 🔴 아직 등록되지 않은 값

| 항목 | 상태 | 이유 · 시점 |
|---|---|---|
| **Scope 1 연료 배출계수** (도시가스·경유·LPG·휘발유) | 미등록 | 사용량 × 순발열량 × 배출계수 × GWP 세 값이 필요하고 순발열량·밀도는 국가별 고시가 다르다. IPCC 2006 Guidelines 원문과 각국 열량환산기준 확인 후 등록. **SMPS 조립공장은 배출량 대부분이 전력이므로 전력 계수만으로 산정·검산이 가능하다** |
| **중국 동북전망 지역값** | 전국 평균 사용 중 | 생태환경부 공고 부속 PDF의 지역·성별 값 확인 후 새 version으로 교체 |
| **환율** (CNY·VND 연평균) | 미등록 | 2026년 연평균은 연도가 끝나야 확정된다. 2027-01 데이터 확정 시점에 등록. 그때까지 집약도 지표는 **"환율 미등록"으로 미산정** 처리되고, 통화가 섞인 합산은 일어나지 않는다 |
| **본사 임대 배분율** | 0.342 가정값 | 임대차계약서 전용면적 확인 후 교체 (EP8 B5) |
| **3법인 회계연도 시작월** | 전부 1월 가정 | 각 법인 재무 확인 (EP8 B1) |
| **법인별 해당 없는 항목** | 기준정보 화면에서 처리 | 담당 배정 탭에서 "적용" 해제 (EP8 B6) |
| **확보 가능 최초 연도** | 전부 `NOT_SURVEYED` | 법인별·항목별 증빙 소재 조사 (EP8 A3) |

계수가 없는 항목은 산출에서 **"미산정"으로 표시되고 0으로 취급되지 않는다.**
등록되는 시점에 자동으로 합산된다.

---

## 변경 규칙

> **W1(2026-09-22~26)에 확정하고 이후 3주간 변경하지 않는다.** (EP8 원칙 4)
> 스키마가 흔들리면 이미 만든 화면을 전부 다시 만들어야 한다.

부득이하게 변경할 때:

1. `schema.sql` 을 고친다 (마이그레이션 파일을 따로 만들지 않는다 — 아직 운영 데이터가 없다)
2. `python3 db/verify.py` 로 제약이 살아있는지 확인한다
3. 로컬에 다시 적용해 본다 (`--local` 로 재생성)
4. 운영 적용 전에 어떤 화면이 영향을 받는지 먼저 확인한다

운영 데이터가 들어간 뒤에는 이 방식이 불가능하다. 그때부터는
`db/migrations/NNN-설명.sql` 을 추가하는 방식으로 바꾼다.
